name: Auth Performance Apply

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'agent/auth-performance-20260724'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout target branch
        uses: actions/checkout@v4
        with:
          ref: agent/auth-performance-20260724
          fetch-depth: 0
          show-progress: false

      - name: Apply authentication performance changes
        id: apply
        shell: bash
        run: |
          set +e
          python3 tools/apply-auth-performance.py > /tmp/auth-apply.out 2> /tmp/auth-apply.err
          status=$?
          cat /tmp/auth-apply.out
          cat /tmp/auth-apply.err >&2
          if [ "$status" -ne 0 ]; then
            {
              echo "status=$status"
              echo "--- stdout ---"
              cat /tmp/auth-apply.out
              echo "--- stderr ---"
              cat /tmp/auth-apply.err
            } > tools/auth-performance-error.txt
          fi
          exit "$status"

      - name: Publish diagnostic on failure
        if: failure() && hashFiles('tools/auth-performance-error.txt') != ''
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/auth-performance-error.txt
          git commit -m "Registrar diagnóstico do aplicador de autenticação" || exit 0
          git push origin HEAD:agent/auth-performance-20260724

      - name: Validate generated sources
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          php -l app/Auth/AuthOnboarding.php >/dev/null
          php -l app/Support/SecurityAccess.php >/dev/null
          php -l app/Runtime/Runner.php >/dev/null
          php -l tools/architecture-check.php >/dev/null
          python3 -m json.tool version.json >/dev/null
          python3 -m json.tool app/update.manifest.json >/dev/null
          python3 -m json.tool app/architecture.manifest.json >/dev/null

      - name: Publish target branch
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && { echo "Nenhuma alteração produzida" >&2; exit 1; }
          git commit -m "Prontoo 1.7.24.5 — otimizar login e logout"
          git push origin HEAD:agent/auth-performance-20260724
