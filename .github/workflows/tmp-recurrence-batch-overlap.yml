name: Temporary recurrence batch overlap guard

on:
  push:
    branches:
      - feature/quick-scheduling-recurrence-1-9-15-1

permissions:
  contents: write

jobs:
  harden:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: none
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Harden and validate
        run: bash tools/tmp-recurrence-batch-overlap
