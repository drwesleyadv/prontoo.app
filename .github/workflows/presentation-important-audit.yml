name: Presentation Important Debt Audit
on:
  push:
    branches: [agent/presentation-important-debt-reduction]
    paths:
      - tools/presentation-important-audit
      - .github/workflows/presentation-important-audit.yml
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/presentation-important-debt-reduction
      - name: Audit exact duplicate rules
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip install --quiet tinycss2
          python3 tools/presentation-important-audit
