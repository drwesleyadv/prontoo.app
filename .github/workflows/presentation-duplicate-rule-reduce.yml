name: Presentation Exact Duplicate Rule Reduction
on:
  push:
    branches: [agent/presentation-important-debt-reduction]
    paths:
      - tools/presentation-duplicate-rule-reduce-patch
      - .github/workflows/presentation-duplicate-rule-reduce.yml
permissions:
  contents: write
jobs:
  reduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/presentation-important-debt-reduction
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: none
      - name: Remove exact duplicate rules and prove UX
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          python3 -m pip install --quiet tinycss2
          python3 tools/presentation-duplicate-rule-reduce-patch
          python3 tools/presentation-duplicate-rule-reduce
          php tools/presentation-css-build --lint
          php tools/presentation-css-build --check
          php tools/presentation-visual-contract-check
          npm install --no-save --no-package-lock playwright@1.59.1
          npx playwright install --with-deps chromium
          node tools/presentation-ux-contract.mjs --check
          python3 - <<'PY'
          import json
          from datetime import datetime
          from zoneinfo import ZoneInfo
          p='version.json'; data=json.load(open(p)); now=datetime.now(ZoneInfo('America/Cuiaba')).replace(microsecond=0)
          data['generated_at_unix']=int(now.timestamp()); data['generated_at']=now.isoformat(); data['updated_at']=now.isoformat(); data['deployment_sync_id']='github-prontoo-1.8.12.11-presentation-exact-duplicate-rule-reduction'; data['deployment_sync_requested_at']=now.isoformat()
          with open(p,'w') as f: json.dump(data,f,indent=4,ensure_ascii=False); f.write('\n')
          PY
          rm -rf node_modules
          git rm -f tools/presentation-important-audit tools/presentation-duplicate-rule-reduce tools/presentation-duplicate-rule-reduce-patch .github/workflows/presentation-important-audit.yml .github/workflows/presentation-duplicate-rule-reduce.yml
          php tools/release-contract-reconcile --write
          php tools/release-contract-reconcile --write
          php tools/release-contract-reconcile --check
          php tools/quality-gate --fast
          git add -A
          git diff --cached --check -- ':!app/Presentation/Styles/**/*.css' ':!public/assets/presentation.css'
          git commit -m "refactor(presentation): remove exact duplicate CSS rules"
          git push origin HEAD:agent/presentation-important-debt-reduction
