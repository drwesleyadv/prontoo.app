'use strict';
const $=selector=>document.querySelector(selector);
const dom={canvas:$('#candleCanvas'),avatarCanvas:$('#avatarCanvas'),avatar:$('#avatar'),favicon:$('#favicon'),price:$('#currentPrice'),change24:$('#change24h'),volumeValue:$('#volume24hValue'),volumeChange:$('#volumeHourlyChange'),carteiraUsdValue:$('#carteiraUsdValue'),carteiraUsdChange:$('#carteiraUsdChange'),pnlValue:$('#pnlValue'),pnlChange:$('#pnlChange'),pnlLabel:$('#pnlLabel'),pnlCard:$('#pnlCard'),priceMin:$('#priceMinValue'),priceMax:$('#priceMaxValue'),priceTrack:$('#priceBarTrack'),pricePtr:$('#priceBarPointer'),volMin:$('#volumeMinValue'),volMax:$('#volumeMaxValue'),volTrack:$('#volumeBarTrack'),volPtr:$('#volumeBarPointer'),rsiPtr:$('#rsiBarPointer'),rsiNum:$('#rsiBarValueNum'),rsiLabel:$('#rsiBarValueLabel'),modal:$('#carteiraModal'),inInteg:$('#inInteg'),inEndereco:$('#inEndereco'),saveBtn:$('#saveCarteiraBtn'),closeBtn:$('#closeCarteiraBtn'),note:$('#carteiraNote'),chartCard:$('#chartCard'),aiCard:$('#aiAnalysisCard'),aiDecisionTitle:$('#aiDecisionTitle'),aiActionIcon:$('#aiActionIcon'),aiSignalStrength:$('#aiSignalStrength'),aiConfidenceFill:$('#aiConfidenceFill'),aiConfidenceText:$('#aiConfidenceText'),aiProbabilities:$('#aiProbabilities'),aiLearningInfo:$('#aiLearningInfo'),aiMetricsGrid:$('#aiMetricsGrid')};
const state={hourly:[],currentPrice:0,previousPrice:0,bestBid:0,bestAsk:0,usdBrl:Number(localStorage.getItem('usd_brl_cache')||0),trend:'neutral',trendDuration:0,aiDecision:'manter',teorica:null,aiEtag:'',socket:null,socketBackoff:1000,lastSocketAt:0,marketRevision:0,sizeRevision:0,chartKey:'',miniKey:'',perpsMarks:{SOL:0,BTC:0,ETH:0},wallet:{integralizado:0,endereco:'',solBalance:0,usdcBalance:0,stakeBalance:0,perpsEquityUsd:0,perpsUnrealizedPnlUsd:0,perpsNotionalUsd:0,perpsCollateralUsd:0,perpsFeesUsd:0,perpsPositionCount:0,perpsAverageEntryPriceUsd:0,perpsPositions:[],perpsStatus:'',perpsUpdatedAt:0}};
const API_BASES=['https://api.binance.com','https://data-api.binance.vision'];
const DIRTY_METRICS=1,DIRTY_CHART=2,DIRTY_MINI=4,DIRTY_AI=8,DIRTY_ALL=15;
let dirty=DIRTY_ALL,renderTimer=0,lastFrameAt=0;
const cuiabaHourFormatter=new Intl.DateTimeFormat('en-GB',{timeZone:'America/Cuiaba',hour:'2-digit',hour12:false});
const fmtPrice=n=>Number(n||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
const fmtUsd=n=>'$ '+Number(n||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});
const fmtBrl=n=>'R$ '+Number(n||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
const fmtVolume=n=>Math.round(Number(n||0)).toLocaleString('pt-BR');
const formatPercent=p=>p==null||Number.isNaN(Number(p))?'Aguardando...':(Number(p)>=0?'▲ ':'▼ ')+Math.abs(Number(p)).toFixed(2).replace('.',',')+'%';
const clamp=v=>Math.max(0,Math.min(100,Number(v)||0));
